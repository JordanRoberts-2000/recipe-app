import SettingsIcon from "../assets/svgs/settings.svg?react";

const Header = ({}) => {
  return (
    <header>
      <div>
        <SettingsIcon />
      </div>
    </header>
  );
};

export default Header;
